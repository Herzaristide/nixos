# Generated by accent-sync — do not edit by hand.
# Loaded via `config-file` from ~/.config/ghostty/config (last entry = wins).
# Remaps palette slots 4 and 12 so every CLI tool that uses blue/bright-blue
# (starship, fastfetch, micro, …) inherits the live accent automatically.

palette = 4=@ACCENT@
palette = 12=@ACCENT@
cursor-color = @ACCENT@

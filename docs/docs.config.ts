import type { DocsConfig } from "barjavel"

const config: DocsConfig = {
  title: "NixOS — Configuration d'Aristide",
  description:
    "Documentation de la configuration NixOS flake-based : quatre hôtes, Hyprland, Quickshell, thème dynamique.",
  lang: "fr",

  // Accent violet, en écho au thème dynamique de la config (accent daemon).
  accent: "#7c3aed",
}

export default config
